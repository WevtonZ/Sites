#include <bits/stdc++.h>

using namespace std;
int main()
{
    int a, b, c, d; // differential numbers.
    
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    int diff = (a*b) - (c*d);

    cout << "DIFERENCA = " << diff << "\n";
}